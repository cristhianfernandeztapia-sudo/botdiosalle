services:
  - type: web
    name: botlia
    env: python
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000 --log-level warning
    plan: free
    autoDeploy: true

  - type: cron
    name: cronlia
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cron_lia.py
    schedule: "*/30 * * * *"
